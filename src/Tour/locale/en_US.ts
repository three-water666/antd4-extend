export interface TourLocale {
  prevText: string;
  nextText: string;
  finishText: string;
}

const locale: TourLocale = {
  prevText: 'Previous',
  nextText: 'Next',
  finishText: 'Finish',
};

export default locale;
